import type {
  ThemeOptions,
  ThemeContext, toggleTheme, useTheme,
} from './ThemeContext';

import ThemeProvider from './ThemeProvider';

export {
  ThemeContext, toggleTheme, useTheme, ThemeOptions, ThemeProvider,
};
